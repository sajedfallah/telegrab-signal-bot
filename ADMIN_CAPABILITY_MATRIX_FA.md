# ماتریس قابلیت‌های مدیریت NEXUS

این سند بر مبنای handlerهای واقعی `app/main.py`، منوهای `app/ui.py`، API وب، دیتابیس و اکسپرت MQL5 تهیه شده است. وضعیت «هماهنگ» فقط زمانی درج شده که هر سه مسیر به یک رکورد مرکزی و Audit مشترک می‌رسند.

## مدل دسترسی

- تلگرام: شناسه کاربر باید در `ADMIN_IDS` باشد. نقش میانی در ربات وجود ندارد.
- وب: نقش‌های `ADMIN` و `MODERATOR` برای عملیات مدیریتی و نقش‌های `VIP_USER` و `REGULAR_USER` برای کاربران تعریف شده‌اند.
- MT5: نقش عملیاتی EA شامل کاربر مجاز و `MT5_ADMIN` است؛ دستورات معامله از جدول مرکزی `autotrade_commands` دریافت می‌شوند.
- نتیجه: نقش `MODERATOR` وب معادل مستقیمی در تلگرام ندارد و باید تا زمان مهاجرت فقط به عملیات کم‌خطر محدود بماند.

## فهرست کامل قابلیت‌های انحصاری ادمین تلگرام

| حوزه | قابلیت واقعی | Callback/Flow | Audit | وضعیت وب/MT5 |
|---|---|---|---|---|
| داشبورد | آمار کاربران، اشتراک، پرداخت و فروش | `admin_stats`, `admin_dashboard` | فقط خواندنی | وب دارد |
| کاربران | فهرست و مشاهده پروفایل، زبان، عضویت، دسترسی‌ها، خرید و امتیاز | `admin_users`, `admuser:*` | فقط خواندنی | وب کامل‌تر است |
| کاربران | افزودن ۳۰ روز، افزودن ۱۰۰ امتیاز، Trial سه‌روزه، لغو VIP | `admextend:*`, `admpoints:*`, `admtrial:*`, `admcancel:*` | دارد | وب فقط مدیریت عمومی/لایسنس را دارد؛ امتیاز و Trial نیازمند تکمیل است |
| کاربران | تولید و ارسال لینک اختصاصی VIP | `admlink:*` | رویداد مستقیم ندارد | وابسته به Telegram API؛ در وب هنوز اجرا نشده |
| پرداخت | فهرست رسیدهای منتظر و تأیید/رد | `admin_pending`, `payok:*`, `payno:*` | دارد | نمایش وب دارد؛ چرخه تأیید/رد و تحویل دسترسی باید از سرویس مشترک فراخوانی شود |
| اشتراک | آمار دسترسی VIP/AutoTrade، تمدید و انقضا | `admin_subs`, `admin_retention` | خواندنی/تمدید دارای Audit | وب دارد |
| پلن | ساخت پلن، قیمت USDT، Setup Fee، دسترسی VIP/AutoTrade، تخفیف تمدید، رتبه ارتقا، فعال/غیرفعال | `admin_plans`, `planadm:*`, `planedit:*`, `planaccess:*`, `planent:*`, `planrenew:*`, `plantoggle:*` و Flowهای پلن | بخشی دارد | وب فعلاً عمدتاً نمایش می‌دهد؛ CRUD کامل نیازمند تکمیل است |
| تخفیف | ساخت، فهرست و فعال/غیرفعال‌کردن کد تخفیف | `admin_discounts`, `discount_create`, `discount_list`, `discount_toggle:*` | دارد | وب فقط نمایش می‌دهد |
| رفرال | تغییر پاداش دعوت، نرخ امتیاز/تخفیف، سقف تخفیف و رتبه‌بندی | `admin_rewards`, `reward_set_*`, `ref_leaderboard` | دارد | وب نیازمند فرم تنظیمات است |
| کمپین | ساخت، انتخاب پلن/مخاطب، سقف مصرف، فهرست و فعال/غیرفعال | `admin_campaigns`, `campaign_*` | دارد | وب نیازمند CRUD است |
| پیام‌رسانی | Broadcast برای همه، VIP، غیرVIP، منقضی و امتیازبالا؛ پیش‌نمایش و تأیید | `admin_broadcast`, `broadcast_target:*`, `broadcast_confirm` | Job مرکزی دارد | ارسال Telegram وب دارد؛ Email/Push تا تنظیم Provider فقط `PARTIAL` می‌ماند |
| AutoTrade | لیست انتظار و درخواست‌های تغییر حساب MT5 | `admin_autotrade`, `admin_mt5_account_changes` | خواندنی | وب Expert/Operations دارد |
| AutoTrade | تأیید یا رد تغییر حساب و اطلاع به مشتری | `admin_mt5_change_review:*` | دارد | API اختصاصی هنوز لازم است |
| کانال | بررسی دسترسی واقعی Bot به Public/Free/VIP | `admin_channels_status` | فقط خواندنی | وب فقط وضعیت عمومی Telegram را نشان می‌دهد |
| قیمت‌گذاری | نرخ دستی تتر/ریال، URL منبع نرخ، TTL فاکتور، Proration و پاک‌کردن Override | `pricing_*` و Flowهای نرخ | دارد | وب نیازمند فرم تنظیمات است |
| سیگنال | ساخت سیگنال شامل بازار، نماد، جهت، تایم‌فریم، نوع سفارش، Entry/SL، TP1..TP10، Risk/Fixed lot، پروفایل تریلینگ و مقصد انتشار | `signal_create`, `sig*`, `sigpublish` | Event/Audit مرکزی | وب دارد؛ MT5 Admin نیز صادر می‌کند |
| سیگنال | مشاهده Active/Closed، Live Sync، آمار و Analytics | `signal_active`, `signal_closed`, `signal_refresh`, `signal_stats`, `signal_analytics` | خواندنی | وب و MT5 Live Sync دارند |
| معامله | Close، Cancel Pending، BE، Partial Close، Update SL/TP و Activate Trailing | `sigact:*` و Flowهای مدیریت | `autotrade_commands` + Audit | وب Operations دقیقاً از همان صف استفاده می‌کند و EA مصرف‌کننده است |
| گزارش | روزانه، هفتگی، آمار کلی، CRM، Audit و آمار سیگنال | `report_*`, `admin_audit` | خواندنی | وب + Excel/PDF دارد |
| سیستم | بکاپ SQLite با نگهداری ۳۰ نسخه | `admin_backup` | دارد | وب مسیر بکاپ جداگانه دارد |
| زبان | تغییر زبان رابط ادمین | `change_language` | ندارد | وب فارسی RTL است |

## چرخه یکپارچه مرجع

1. هر Mutation از Telegram/Web/MT5 باید ابتدا در دیتابیس مرکزی ثبت شود.
2. عملیات معامله فقط با `autotrade_commands` و `signal_events` اجرا شود؛ پنل‌ها مستقیماً وضعیت معامله را موفق اعلام نکنند.
3. EA نتیجه واقعی Broker را گزارش کند؛ `ticket`, `deal_id`, `event_id` و حجم قبل/بعد مبنای حقیقت هستند.
4. WebSocket و Telegram اطلاعات را از همان رکورد مرکزی بخوانند.
5. هر Mutation دارای `actor_type`, `actor_id`, `target_id`, payload خلاصه، زمان و نتیجه باشد.
6. اعلان شکست‌خورده نباید Mutation مالی یا معاملاتی موفق را Rollback یا پنهان کند؛ وضعیت تحویل جداگانه ثبت شود.

## شکاف‌های جلوگیری‌کننده از Production

- منطق تأیید پرداخت تلگرام علاوه بر تغییر status، لایسنس، رزرو امتیاز/تخفیف، دعوت VIP، درخواست حساب MT5 و اعلان‌ها را مدیریت می‌کند. endpoint وب نباید این منطق را کپی کند؛ ابتدا باید به سرویس دامنه مشترک منتقل شود.
- Email و Push بدون Provider و subscription واقعی قابل تحویل نیستند و سیستم فعلی عمداً موفقیت جعلی گزارش نمی‌کند.
- MQL4 در ریپو وجود ندارد؛ محصول فعلی MQL5 است. اعلام پشتیبانی MQL4 بدون پیاده‌سازی و تست جداگانه مجاز نیست.
- تست واقعی Partial Close به حساب Demo، مشخصات نماد و بازار باز نیاز دارد. تست خودکار نباید روی حساب واقعی معامله ایجاد کند.
- VPS فعلی تا پایان Migration دست‌نخورده می‌ماند؛ دیتابیس آن نباید مستقیماً با schema جدید جایگزین شود.
