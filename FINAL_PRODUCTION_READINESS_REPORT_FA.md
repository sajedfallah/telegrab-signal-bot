# گزارش نهایی آمادگی NEXUS v0.6.5

## نتیجه

**آماده‌ی بازآزمایی کنترل‌شده روی دمو**

## شواهد انجام‌شده

- مجموعه‌ی تست خودکار: `229 passed`.
- آخرین سورس EA با MetaEditor: صفر خطا و صفر هشدار.
- اجرای واقعی روی حساب دمو ePlanet با نماد `XAUUSD.ec` انجام شد؛ دریافت سیگنال، اجرای بازار، partial close سی‌درصدی و همگام‌سازی live truth مشاهده شد.
- پس از پاک‌شدن comment توسط بروکر، بازیابی signal code با ticket پایدار در سورس پیاده‌سازی شد.
- احراز هویت توکن نامعتبر Unicode اکنون خطای کنترل‌شده می‌دهد و باعث HTTP 500 نمی‌شود.
- اگر MT5 نتواند screenshot بگیرد، انتشار به‌جای تصویر خالی یک Signal Card خوانا می‌فرستد.
- رویداد CLOSE در صورت race با انتشار پس‌زمینه، ابتدا anchor سیگنال را ایجاد/بازیابی می‌کند و سپس نتیجه را به پیام اولیه reply می‌کند.

## مورد باقی‌مانده

مسیر trailing در سورس و partial-close در اجرای دمو مشاهده شده است؛ حرکت پویا و مستقل SL برای همه‌ی هفت پروفایل هنوز تست پوششی کامل روی تیک‌های بازار ندارد و باید در بازآزمایی دمو ثبت شود. همچنین سه سناریوی عملی باید تأیید شوند: وجود تصویر/Signal Card در کانال، reply نتیجه پس از TP/SL، و recovery پس از پاک‌شدن comment توسط بروکر.

## راه‌اندازی

1. مقادیر واقعی را فقط در `.env` محلی نگه دارید.
2. API را با `python -m uvicorn app.autotrade.api:app --host 127.0.0.1 --port 8080` اجرا کنید.
3. EA کامپایل‌شده را در `MQL5/Experts/NEXUS_AutoTrade/` نصب کنید، Algo Trading و WebRequest به `http://127.0.0.1:8080` را فعال کنید.

توکن تلگرام در یک خروجی تصویری میانی قابل مشاهده شد؛ پیش از استفاده‌ی واقعی حتماً آن را در BotFather rotate کنید.
