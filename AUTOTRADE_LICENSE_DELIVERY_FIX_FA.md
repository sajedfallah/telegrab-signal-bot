# NEXUS Auto Trade - License Delivery Fix

این Build رفتار تأیید پرداخت را اصلاح می‌کند:

- پس از تأیید پرداخت، همان سرویس License موجود `create_or_extend_license` اجرا می‌شود.
- اگر پلن دارای Auto Trade باشد، License Key به‌صورت خودکار برای کاربر در تلگرام ارسال می‌شود.
- در تمدید، License Key قبلی حفظ و فقط تاریخ اعتبار تمدید می‌شود.
- بخش «سیگنال + اتوترید» همیشه License فعال را به کاربر نمایش می‌دهد تا در صورت حذف پیام اولیه بتواند دوباره آن را ببیند.
- هر License برای اتصال به یک حساب MT5 طراحی شده است.
- فایل `.env` با تنظیمات ارسالی کاربر در ریشه Build قرار دارد و Auto Trade API روی پورت 8080 تنظیم شده است.
