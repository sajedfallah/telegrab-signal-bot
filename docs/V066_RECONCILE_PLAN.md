# NEXUS v0.6.6 release reconciliation

Base: `release/nexus-v0.6.5` production head.

This branch reconciles the v0.6.6 Mini App setup/fixed-volume and live MT5 PnL work onto the production release line without replacing the production history.

The isolated runtime smoke test against the production SQLite database passed before reconciliation.
