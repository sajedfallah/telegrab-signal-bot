# NEXUS v7.1.1 — Final Subscription UX & Localization

## اشتراک‌ها

ساختار خرید اشتراک به یک صفحه واحد تبدیل شد و دقیقاً سه محصول تجاری دارد:

1. **VIP**
2. **AutoTrade Expert**
3. **VIP + AutoTrade Expert**

کاربر دارای VIP می‌تواند بعداً فقط AutoTrade Expert را خریداری کند و کاربر دارای AutoTrade نیز می‌تواند VIP را جداگانه اضافه کند.

Bundle به‌عنوان ترکیب دو دسترسی VIP و AutoTrade نگهداری می‌شود و منطق ارتقا با اعتبار باقی‌مانده (Proration) حفظ شده است.

## حساب کاربری

- پرداخت‌های من
- دعوت دوستان
- تمدید / ارتقای اشتراک
- لینک ورود VIP
- تغییر زبان

«دعوت دوستان» و «پرداخت‌های من» از منوی اصلی حذف و زیر «حساب من» قرار گرفتند.

## قیمت‌گذاری

قیمت‌ها در رابط کاربری به‌صورت ثابت نوشته نمی‌شوند و از Plan Management خوانده می‌شوند.

پلن‌های مستقل AutoTrade Expert با کدهای `AEX1M`, `AEX3M`, `AEX6M`, `AEX12M` اضافه شده‌اند و عمداً `SET_PRICE` هستند تا قیمت تجاری واقعی بدون حدس وارد شود.

پلن‌های `AUTO1M` تا `AUTO12M` برای حفظ سازگاری با نسخه قبلی به‌عنوان Bundle باقی مانده‌اند.

## زبان

رابط فارسی و انگلیسی از طریق انتخاب صریح زبان کنترل می‌شود. منوهای فارسی از متن‌های انگلیسی ناخواسته پاک‌سازی شده‌اند و نام‌های فنی/برند مانند `VIP` و `AutoTrade Expert` به‌عنوان نام محصول حفظ شده‌اند.

## پرداخت

اصلاح مستقل بودن پرداخت USDT از نرخ USDT/RIAL در نسخه قبلی حفظ شده است.

## تست

- Python compile: PASS
- Build validation: PASS
- Static subscription/UI checks: PASS
- MT5 EX5: نیازمند Compile واقعی در MetaEditor روی Windows
