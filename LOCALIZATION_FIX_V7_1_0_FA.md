# NEXUS v7.1.0 — اصلاح کامل رابط فارسی / انگلیسی

## هدف
تمام متن‌های قابل مشاهده در رابط فارسی NEXUS بررسی شد تا منوی فارسی، زیرمنوها، دکمه‌ها، پیام‌های پرداخت، Auto Trade، Signal Center، گزارش‌ها و تنظیمات نرخ به فارسی نمایش داده شوند.

زبان انگلیسی فقط وقتی نمایش داده می‌شود که کاربر/ادمین از «تغییر زبان → English» استفاده کند.

## اصلاحات اصلی
- منوی اصلی پنل ادمین کاملاً فارسی شد.
- زیرمنوهای مالی، پرداخت، پلن‌ها، تخفیف، رفرال، کمپین، گزارش‌ها و تنظیمات سیستم فارسی شدند.
- منوی «نرخ تتر/ریال» و تمام گزینه‌های آن فارسی شد:
  - تغییر نرخ دستی تتر
  - مدت اعتبار فاکتور
  - منبع نرخ
  - فعال/غیرفعال‌کردن محاسبه اعتبار ارتقا
  - حذف نرخ دستی
- دکمه‌های تأیید/رد پرداخت بر اساس زبان ادمین نمایش داده می‌شوند.
- متن‌های Auto Trade، وضعیت اتصال، لایسنس، نصب MT5 و اتصال صرافی در حالت فارسی ترجمه شدند.
- برچسب‌های مدیریت سیگنال، حدضرر متحرک، سر‌به‌سر، بستن بخشی، تغییر حدسود/حدضرر و سفارش لیمیت فارسی شدند.
- متن‌های Referral/Points، Campaign، Broadcast و CRM فارسی شدند.
- یک تست Localization اضافه شد تا از نشت اصطلاحات انگلیسی رایج به منوهای فارسی جلوگیری کند.

## تست
- `py_compile`: PASS
- تست‌های اصلی پروژه: **64 passed**
- تست دود رابط فارسی: **PASS**
- در محیط بررسی فعلی، تست UI رسمی به دلیل نبود `aiogram` در محیط تست محلی Skip می‌شود؛ روی سرور پروژه `aiogram` نصب است و تست باید اجرا شود.

## نکته
نام‌های فنی و شناسه‌های ضروری مانند NEXUS، MT5، API، EA، ATR و TXID عمداً حفظ شده‌اند چون نام/استاندارد فنی هستند، نه زبان رابط کاربری.
