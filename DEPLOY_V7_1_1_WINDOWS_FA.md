# NEXUS v7.1.1 — راهنمای استقرار Windows

## تغییرات اصلی

- منوی اصلی خلوت و واحد شد.
- خرید اشتراک فقط از مسیر «💎 خرید اشتراک» انجام می‌شود.
- سه محصول:
  1. VIP
  2. AutoTrade Expert
  3. VIP + AutoTrade Expert
- کاربر دارای VIP می‌تواند AutoTrade Expert را جداگانه اضافه کند.
- کاربر دارای AutoTrade Expert می‌تواند VIP را جداگانه اضافه کند.
- Referral و Payments زیر «حساب من» قرار گرفتند.
- AutoTrade از منوی اصلی به صفحه واقعی وضعیت/مدیریت هدایت می‌شود.
- قیمت‌ها از Plan Management خوانده می‌شوند.
- پلن‌های مستقل AutoTrade Expert با کدهای AEX1M/AEX3M/AEX6M/AEX12M ایجاد می‌شوند و تا تعیین قیمت کسب‌وکار `SET_PRICE` هستند.
- AUTO1M/AUTO3M/AUTO6M/AUTO12M برای سازگاری با قیمت‌ها و دسترسی‌های قبلی به‌عنوان Bundle باقی می‌مانند.

## نصب روی سرور

1. نسخه قبلی را متوقف کنید.
2. از `.env` و دیتابیس فعلی نسخه پشتیبان بگیرید.
3. ZIP را در یک پوشه جدید استخراج کنید.
4. `.env` نسخه قبلی را به ریشه نسخه جدید منتقل کنید؛ آن را از ZIP عمومی کپی نکنید.
5. در CMD:
   `python -m venv .venv`
   `.\.venv\Scriptsctivate`
   `python -m pip install -r requirements.txt`
6. برای اجرای کامل:
   `start_all_windows.bat`
7. برای اجرای فقط Bot:
   `start_windows.bat`

## تنظیم قیمت AutoTrade Expert

در پنل مدیریت:
`پنل مدیریت → کاربران و اشتراک‌ها / مالی و پرداخت → مدیریت پلن‌ها`

پلن‌های AEX را پیدا کنید و قیمت واقعی 1، 3، 6 و 12 ماهه را تنظیم و فعال کنید.

تا قبل از تعیین قیمت، پلن AEX در انتخاب مدت قابل خرید نمایش داده نمی‌شود و سیستم به کاربر اجازه ایجاد فاکتور با `SET_PRICE` نمی‌دهد.

## تست

پس از راه‌اندازی:

- `/start`
- 💎 خرید اشتراک
- بررسی سه محصول
- خرید VIP
- خرید مستقل AutoTrade Expert برای حساب دارای VIP
- بررسی ارتقای Bundle
- 👤 حساب من
- 💳 پرداخت‌های من
- 🎁 دعوت دوستان
- 🌐 تغییر زبان

## MT5

سورس EA در پروژه موجود است، اما EX5 باید با MetaEditor روی همان Windows Server مجدداً Compile شود. تا زمانی که Compile و اتصال MT5 تست نشده، معامله واقعی انجام ندهید.
