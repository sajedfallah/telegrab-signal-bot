# چرخه صدور سیگنال از وب‌پنل

صدور سیگنال وب، مینی‌اپ، ربات و اکسپرت از یک رکورد مشترک و یک چرخه قابل‌ردیابی استفاده می‌کند.

## مسیر وب‌پنل

1. ادمین مشخصات سیگنال و حساب مرجع MT5 را انتخاب می‌کند.
2. تصویر چارت PNG/JPEG تا سقف ۵ مگابایت اختیاری است.
3. API اعتبار قیمت‌ها، جهت، SL، TP، حجم/ریسک، تریلینگ و تصویر را بررسی می‌کند.
4. سیگنال با `issuer_type=MT5_ADMIN` و وضعیت `ACTIVE` ثبت می‌شود تا اکسپرت مرجع آن را دریافت کند.
5. تصویر انتخاب‌شده به‌صورت موقت و مرتبط با شناسه سیگنال نگهداری می‌شود.
6. اکسپرت سفارش را اعتبارسنجی/اجرا می‌کند و رسید `EXECUTED` یا `PENDING` می‌فرستد.
7. فقط پس از رسید پذیرفته‌شده، انتشار تلگرام آغاز می‌شود.
8. تصویر چارت با قاب NEXUS منتشر می‌شود. اگر تصویر وجود نداشته باشد، از اطلاعات سیگنال یک فلش‌کارت استاندارد ساخته می‌شود.
9. شناسه پیام FREE/VIP، رسید MT5 و رویداد انتشار در دیتابیس ثبت می‌شوند.

این ترتیب مانع اعلام اجرای موفق در تلگرام پیش از تأیید واقعی متاتریدر می‌شود. ارسال تکراری رسید نیز به‌دلیل claim اتمیک کانال باعث انتشار تکراری نخواهد شد.

## تنظیم ضروری

حساب یا حساب‌های مرجع ادمین باید در متغیر زیر تعریف شوند:

```env
NEXUS_ADMIN_MT5_ACCOUNTS=80150619
NEXUS_ADMIN_TOKEN=CHANGE_TO_A_LONG_RANDOM_SECRET
```

همان حساب باید اکسپرت NEXUS را در حالت Admin اجرا کند. وب‌پنل فهرست حساب‌های مجاز را در فرم صدور نمایش می‌دهد.

