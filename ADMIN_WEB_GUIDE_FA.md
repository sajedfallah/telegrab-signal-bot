# راه‌اندازی پنل مدیریت وب NEXUS

## آماده‌سازی

در فایل `.env` این متغیرها را با مقادیر امن تنظیم کنید:

```env
ADMIN_WEB_USERNAME=admin
ADMIN_WEB_PASSWORD=یک-رمز-تصادفی-حداقل-۱۲-کاراکتری
ADMIN_WEB_SECRET=یک-کلید-تصادفی-حداقل-۳۲-کاراکتری
ADMIN_WEB_ORIGINS=https://admin.example.com
ADMIN_SESSION_MINUTES=480
```

اگر API پشت reverse proxy عمومی قرار می‌گیرد، HTTPS اجباری است. مقدارهای نمونه‌ی `CHANGE_ME` حساب مدیر ایجاد نمی‌کنند.

## ساخت رابط کاربری

```powershell
cd admin-web
npm install
npm run build
cd ..
python run_api.py
```

سپس پنل در `http://127.0.0.1:8080/admin/` و مستندات REST در `http://127.0.0.1:8080/docs` در دسترس است.

برای توسعه رابط کاربری، API را روی پورت 8080 اجرا کنید و در پوشه `admin-web` دستور `npm run dev` را بزنید.

## نکات عملیاتی

- Telegram مستقیماً با توکن فعلی ربات ارسال می‌شود.
- Email و Web Push تا زمان تعریف سرویس‌دهنده و اطلاعات اشتراک، با وضعیت `PARTIAL` در صف ثبت می‌شوند.
- حذف کاربری که سابقه مالی دارد عمداً ممنوع است؛ چنین کاربری باید مسدود شود.
- حذف سیگنال فقط در وضعیت پیش‌نویس یا لغوشده مجاز است.
- فایل دیتابیس قبل از مهاجرت اولیه نسخه پشتیبان داشته باشد.
